1. For an saved yarn order, it's not possible to update it! Add update flow to it - **Completed**
2. In the In-house section, under the Fabric Stock tab, I want to add a new button called the add existing stock, so clicking that pop-up should open a new tab on which it should allow the user to select the dia and style and add the kg's. This added data should reflect under the same fabric in stock section. Also maintain this added existing stock as a separate table in the UI by marking this entry with a flag column or something. In this new "Added stock" tab, show the date, dia, style and kg's added as an entry. - **Completed**
3. Sometimes, in the in-house splitting, the save is not happening properly while entering a split, but it works fine when I refresh the whole browser screen. - **Completed**
4. There are places where the size is hardcoded, like XS,S,M,L, etc. To that, I want you to add 2xl to it. This should be reflected across all places. One such place is in cutting, where the size is mentioned - **Completed**
5. In the cutting and stitching section, while creating a new entry, add a new field called "Rate/pcs" which should be a number field for the user to enter numbers and show it on the list page also. - **Completed**
6. In stitching: also, after saving a record, there is no option to edit it, so we add an option to edit the saved record also. - **Completed**
7. Currently, in the Create Cutting section, for each cutting row there is a field called Output Pieces. We do not want it. For each field, make it a separate field after the Cutting Row section so that the user can enter a single number of output pieces. - **Completed**
